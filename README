~ harry's dotfiles ~

fish

    mv ~/.config/fish ~/.config/fish.bak
    ln -s ~/.dotfiles/fish ~/.config/fish

    Local machine overrides are in `~/.dotfiles/fish/local.fish` - they are not shared.

git

  # ~/.gitconfig
  [include]
    path = ~/.dotfiles/gitconfig

bash

  # ~/.bashrc
  . ~/.dotfiles/bashrc

vim

  # ~/.vimrc
  so ~/.dotfiles/vimrc
